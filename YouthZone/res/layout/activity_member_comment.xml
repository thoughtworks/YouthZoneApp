<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.thoughtworks.youthzone.MemberCommentActivity" >
    
    <EditText
        android:id="@+id/member_comment"
        android:layout_width="wrap_content"
        android:layout_height="200dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:background="#DDDDDD"
        android:ems="10"
        android:gravity="top|start"
        android:inputType="textMultiLine"
        android:maxLength="250"
        android:labelFor="@+id/member_comment" >

        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/save_member_comment_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/member_comment"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="93dp"
        android:onClick="onSaveCommentClick"
        android:text="@string/save_comment"
        android:background="@drawable/onside_button_selector"
        android:textAppearance="@style/OnsideButton" />

</RelativeLayout>
